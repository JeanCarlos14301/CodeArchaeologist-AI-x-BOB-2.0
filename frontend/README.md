# Frontend

Vite + React + Tailwind. El build se sirve como estáticos desde FastAPI.

- **Dueño:** Edgar (E-01 a E-08).

## TODO
- [ ] Inicializar el proyecto (E-01).
- [ ] Documentar `npm run dev` y `npm run build`.
